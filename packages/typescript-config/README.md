# `@guerillaglass/typescript-config`

Shared TypeScript configuration presets for the workspace.

`base.json` defines the strict NodeNext baseline used by package builds and contract libraries.

`react-library.json` extends the base config with `react-jsx` for packages that ship React
components or renderer-side helpers.
