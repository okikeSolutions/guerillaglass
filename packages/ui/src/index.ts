/**
 * Root entrypoint for shared UI helpers.
 *
 * Component primitives are intentionally consumed through subpath exports such as
 * `@guerillaglass/ui/components/button`, while cross-cutting helpers can be re-exported here.
 */
export * from "./lib/utils";
